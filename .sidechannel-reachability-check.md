# Reachability check marker (throwaway)

This file was written to memento through the GitHub API by a session whose
working directory was a different, unrelated git repo — no memento clone in
cwd, no `git push`. It exists only to prove the cross-repo phone-home write
path is reachable. The branch carrying it is deleted immediately after the
check.
