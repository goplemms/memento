---
name: skill-name
description: One or two sentences describing what this skill does and when to use it. This text is how Claude decides to invoke the skill, so make the trigger explicit ("Use when …").
---

<!--
Save this as skills/<skill-name>/SKILL.md so the plugin loads it namespaced
(memento:<skill-name>). The YAML frontmatter above is REQUIRED — a SKILL.md
without `name` and `description` does not load.
-->

# Skill Name

## Purpose

What single task this skill helps with.

## Inputs

- What the agent needs to begin

## Process

1. Step one
2. Step two
3. Step three

## Outputs

What a good result should include.

## Notes

Claude Code specific hints, limits, or reminders.
