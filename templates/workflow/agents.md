# Agents: <FEATURE_NAME>

OPT-IN. Per-subagent scope for the CURRENT milestone only. Rewrite when the
active milestone changes — this is not a historical log.

---

## Agent: <role/name>

- **Concern:** The one thing this subagent owns.
- **Files:** Paths it may read and write.
- **Do not touch:** Paths or concerns explicitly out of bounds.
- **Exit criteria:** What "done" looks like for this subagent.
